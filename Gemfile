# frozen_string_literal: true

source "https://rubygems.org"
gemspec

gem "csv"
gem "base64"
gem "logger"
gem "bigdecimal"

gem "jekyll-seo-tag"
gem "jekyll-sitemap"

# Needed to resolve `remote_theme:` when building locally.
# GitHub Pages injects this itself, and it is on their plugin whitelist,
# so it is safe for the deployed build too.
gem "jekyll-remote-theme"
